(function () {
  const ImageHandler = {
    init() {
      console.info("[ImageHandler] Placeholder initialized. Image upload coming in Phase 3.");
    },
  };

  window.ImageHandler = ImageHandler;
})();
